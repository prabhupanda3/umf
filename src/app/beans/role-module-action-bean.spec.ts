import { RoleModuleActionBean } from './role-module-action-bean';

describe('RoleModuleActionBean', () => {
  it('should create an instance', () => {
    expect(new RoleModuleActionBean()).toBeTruthy();
  });
});
